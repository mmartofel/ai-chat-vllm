apiVersion: v1
kind: Service
metadata:
  name: model-vllm
  namespace: ${NAMESPACE}
  labels:
    app: model-vllm
spec:
  type: ClusterIP
  selector:
    app: model-vllm
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
